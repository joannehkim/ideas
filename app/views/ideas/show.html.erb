<nav class="navbar navbar-fixed-top navbar-dark bg-inverse">
  <span class="navbar-brand">Ideas</span>

  <form action="/sessions/<%= session[:user_id] %>" method="post">
  <input type='hidden' name='authenticity_token' value='<%= form_authenticity_token %>'>
  <input type='hidden' name='_method' value='delete'>
  <ul class="nav navbar-nav navbar-right">
  <button class="btn btn-primary btn-md navbar-nav navbar-btn" id="logout_button" type="submit">Log out</button>
  </ul>
  </form>
  <a href="/ideas"><button class="btn btn-primary btn-md navbar-nav navbar-btn" id="ideas_button" type="submit">Ideas</button></a>
</nav>

<div class="container">
  <div class="starter-template">
  	<h1><%= @idea.user.name %> says:</h1>
  	<h3 class="content">"<%= @idea.content %>"</h3>
  	<hr>

  	<h4>People who liked this post:</h4>



  <div class="col-md-4">
  </div>
  <div class="col-md-4">
  	<div class="table-responsive">
  <center>
            
  <table class="table">
    <thead>
      <tr>
        <th>Alias</th>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
    <% @ideas_liked.each do |i| %>
      <tr>
        <td><a href="/users/<%= i.id %>"><%= i.alias %></a></td>
        <td><%= i.name %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
  </div>
  <div class="col-md-4">
  </div>

  </div>
</div>